# testproject-landingpage

You are working in a **test project** inside theorchestra's smoke-test harness.

## Rules (non-negotiable)

1. **Do ONLY the task assigned to your role.** Do not refactor, do not wander.
2. Write the ONE file your task specifies. Keep it small (<80 lines).
3. When done, emit an A2A `type=result` envelope on your final line per the `[PEER-PANE CONTEXT]` header that Claude Code gives you at startup.
4. Then write one `DECISION: <verb> — <reason>` line.
5. Stop.

## Your role-specific task is in your initial prompt

The prompt from your coordinator tells you which file to write and what to put in it.

No git commits, no pushes, no deploys. File-system-only test.
