# theorchestra v3.0 — Phased Implementation Plan

- **Status**: Draft (2026-04-19)
- **Baselines**: `docs/v3.0-spec.md`, ADR-001 through ADR-005, `docs/v3.0-decisions.md`
- **Branch**: `v3.0` (off `main`, diverges from commit f59236b)
- **Port during dogfood**: :4300 (v2.7.0 keeps :4200)

## Strategy

Thin vertical slices. Each phase stands up a working end-to-end path for one subsystem, ships with a verification gate, and commits on the `v3.0` branch. No phase starts without the prior phase's gate passing. Parallel work is allowed within a phase (tracked as subtasks); cross-phase parallelism is not allowed until Phase 6.

- **Definition of "gate passed"**: every acceptance check in the phase gate executes (test or documented manual procedure) and produces the expected result. Documented in a `docs/v3.0-phase-<N>-gate.md` artifact with paste-able command output.
- **Definition of "thin slice"**: the smallest scope that lets us exercise the subsystem end-to-end for real.
- **Rollback criterion**: every phase must leave the v3.0 branch in a runnable state. If a phase goes more than 2 working days over estimate without a runnable state, split the phase.

Estimate notation: "S" = ≤1 day, "M" = 2-4 days, "L" = ≤1 week. Estimates are effort, not wall-clock.

---

## Phase 1 — Substrate core

**Goal.** One Claude Code pane spawns inside the dashboard process via node-pty, renders in a browser tab via xterm.js, accepts input, survives a browser refresh.

**Deliverables.**
- `src/backend/pty-manager.ts` — node-pty spawn + ring buffer (10k lines) + data broadcast
- `src/backend/ws-server.ts` — WebSocket endpoint `/ws/pty/:session_id`, xterm `AttachAddon`-compatible wire protocol
- `src/frontend/` — Vite + React + TS scaffolding with one `<Terminal />` (@wterm/react) component on a `/` route
- `scripts/theorchestra-start.ts` — launches backend (port :4300)
- Minimal session registry (in-memory `Map<session_id, PtyHandle>`)

**Depends on.** Nothing. Branch creation: `git checkout -b v3.0`.

**Gate (Phase 1). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-1-gate.md`.**
- [x] `theorchestra start` launches on :4300
- [x] Visiting `http://localhost:4300` shows one xterm rendering a live process (default is `cmd.exe`/`bash`; `claude` behind `THEORCHESTRA_DEFAULT_CLI=claude` — see gate doc §Deviation)
- [x] Typing into xterm writes to the process, output renders back
- [x] Browser refresh: scrollback restored via backend ring-buffer replay
- [ ] 24h process uptime with 1 active pane, zero crashes, zero 10054 events — DEFERRED to Phase 10 dogfood (cannot run soak in build session). Zero-10054 holds by construction per ADR-001.
- [x] Gate doc: `docs/v3.0-phase-1-gate.md`

**Estimate.** M (2-4 days).

---

## Phase 2 — MCP tool parity

**Goal.** All 16 MCP tools (FR-MCP) reimplemented against the node-pty backend with v2.7.0 signature compatibility. Existing Claude Code sessions can use `mcp__wezbridge__*` tools pointed at v3.0.

**Deliverables.**
- `src/mcp/server.ts` — MCP server emits the 16 tools from `src/mcp-server.cjs`, identical schemas
- Each tool implementation hits the dashboard backend over loopback HTTP (reusing the token, so MCP tools are just thin clients of the HTTP API)
- Tool-by-tool integration tests: spawn → send_prompt → read_output → kill_session per tool

**Depends on.** Phase 1.

**Gate (Phase 2). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-2-gate.md`.**
- [x] `discover_sessions` returns live pane list (evidence: backend + MCP gate)
- [~] `spawn_session { persona: 'reviewer' }` opens a new pane with the reviewer persona preloaded — partial: wiring complete (persona resolved + `--append-system-prompt-file` passed + record tracks persona field); full Phase-5 verification deferred
- [x] `send_prompt` + `send_key('enter')` delivers a prompt — evidence in both gate harnesses; note v3.0 `send_prompt` already appends `\r`, so the extra enter is belt-and-suspenders
- [~] `auto_handoff { pane_id }` — MCP tool registered and returns structured `pending_phase_4` payload; full impl is Phase 4 dependency
- [x] Every tool has a round-trip test — 11 backend + 5 MCP assertions, 16/16 passing
- [ ] Regression sweep against v2.7 Claude pane reconfigured to v3.0 MCP — DEFERRED to Phase 10 dogfood
- [x] Gate doc: `docs/v3.0-phase-2-gate.md`

**Estimate.** L (≤1 week).

---

## Phase 3 — Agent-browser + SSE event bus

**Goal.** Replace `omni-watcher.cjs`'s 30s regex-scrollback poll with agent-browser a11y-tree reads + push-based SSE events. Omniclaude can subscribe to the event bus.

**Deliverables.**
- `src/backend/events.ts` — canonical SSE event schema: `pane_idle`, `permission_prompt`, `peer_orphaned`, `ctx_threshold`, `a2a_received`, `pane_stuck`, `task_dispatched`, `task_completed`
- `src/backend/event-emitters/*.ts` — one per event type, reading from the PTY data stream, ring buffer, or `active_tasks.md` watcher
- agent-browser Rust CLI integration: sidecar spawn on startup, stdin/stdout JSON batch protocol
- Client library for agent-browser: `src/backend/agent-browser-client.ts` with `readPane(session_id)` and `readAll([session_id...])`
- a11y mode enabled on every `<Terminal />` with stable DOM id `pane-<session_id>`

**Depends on.** Phase 1 (PTY data stream) and Phase 2 (session registry).

**Gate (Phase 3). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-3-gate.md`. Pivoted from Rust agent-browser to `@xterm/headless` per `docs/adrs/v3.0-003-addendum-xterm-headless.md`.**
- [x] All 8 SSE event types fire within timeout — `scripts/v3-phase3-gate.ts` 8/8 PASS
- [x] Batch-read performance — trivial with in-process `@xterm/headless` (sub-millisecond per pane, no RPC hop)
- [x] a11y read integrity contract — `_injectForTest` drives canned ANSI into the headless terminal; emitter detection is the contract test
- [x] `omni-watcher.cjs` removal — file deleted from v3.0 tree along with the `"watcher"` npm script. v3.0 backend ported its heuristics into `src/backend/event-emitters/{status-bar,a2a-scanner}.ts`. v2.7 users stay on the `v2.7.0` git tag.

**Estimate.** L (≤1 week). Agent-browser distribution is the biggest unknown; if blocked, treat the Rust binary as a user-installed prerequisite and defer packaging to Phase 10.

---

## Phase 4 — Auto-handoff + A2A protocol carryover

**Goal.** Carry over the v2.x A2A envelope rules and auto-handoff daemon on the new substrate.

**Deliverables.**
- `src/backend/a2a.ts` — envelope parser (`[A2A from pane-X to pane-Y | corr=... | type=...]`) feeding the `a2a_received` SSE event, tracking open corr IDs and emitting `peer_orphaned`
- `src/backend/auto-handoff.ts` — daemon honouring the 30% suggest / 50% enforce thresholds; READY/NOT_READY readiness-check protocol (120s poll); wait-for-idle before `/clear`; Ctrl+C + double-enter
- UI: handoff suggest toast (30%) and enforce modal (50%) — React components
- `/handoff` skill invocation surface preserved; external-write fallback for ctx-full panes
- Per-project denylist respected (`vault/_orchestrator-config.md`)

**Depends on.** Phase 2 (MCP tools for send_prompt/send_key, auto_handoff), Phase 3 (ctx_threshold event).

**Gate (Phase 4). STATUS: SURFACE VERIFIED 2026-04-19 — see `docs/v3.0-phase-45-gate.md`. FULL FLOW NOT YET RUN WITH REAL CLAUDE.**
- [ ] 3 simulated high-ctx panes run the auto-handoff end-to-end, producing the 7-section template — **NOT RUN** (needs a real Claude pane to author the handoff file; deferred to Phase 10 dogfood)
- [x] A2A envelope scanner + orphan detection — Phase 3 gate 8/8 PASS
- [x] Orphan detection within 5s — Phase 3 gate
- [x] Dual-enter rule at MCP layer — Phase 2 gate

**Estimate.** M (2-4 days).

---

## Phase 5 — Agency mode

**Goal.** Persona-specialised pane spawn, git-worktree isolation, PRD-driven team bootstrap, `[PEER-PANE CONTEXT]` bootstrap prefix.

**Deliverables.**
- `src/backend/personas.ts` — reads `~/.claude/agents/` (Claude) + equivalent Codex source; returns persona system prompt to inject at spawn time
- `src/backend/worktree.ts` — on `spawn_session { worktree: true }`, runs `git worktree add <path> <branch>` in the target repo before spawning
- `src/backend/prd-bootstrap.ts` — reads a PRD YAML (role → task → persona), spawns the whole team in parallel
- `[PEER-PANE CONTEXT]` header auto-injection when `spawned_by_pane_id` is provided (copy v2.7 implementation)
- Spawn wizard UI: persona picker (95+ choices), permission-mode selector (plan / acceptEdits / default / bypassPermissions), worktree toggle

**Depends on.** Phase 2 (spawn_session), Phase 4 (A2A for peer-context addressing).

**Gate (Phase 5). STATUS: BACKEND VERIFIED, UI NOT BUILT — see `docs/v3.0-phase-45-gate.md`.**
- [x] Spawn wizard UI — shipped in U5 (commit cf327c2); searchable 65-persona picker from `/api/personas`, 2×2 permission-mode radio (default/plan/acceptEdits/bypassPermissions), worktree toggle, cwd input
- [x] Worktree spawn primitive — `/api/worktree` add+remove verified
- [x] PRD bootstrap backend — 3-role YAML spawns 3 panes via `/api/prd-bootstrap`
- [ ] `[PEER-PANE CONTEXT]` header appears in live pane's first message — **CODE PORTED, NOT LIVE-VERIFIED** (needs real Claude pane dogfood)

**Estimate.** M (2-4 days).

---

## Phase 6 — PTY durability

**Goal.** Satisfy FR-Persist end-to-end: browser refresh, dashboard restart, host reboot, multi-device.

**Deliverables.**
- `src/backend/session-manifest.ts` — writes `vault/_sessions/<session_id>.json` on spawn and status change
- On dashboard startup: scan manifests, respawn panes where PTY is dead (all panes on Windows; POSIX may reattach where possible — best-effort)
- Respawn registry: `persona → resume command` mapping (Claude: `claude --continue`, Codex: documented resume flag, others: fresh)
- xterm SerializeAddon integration on the client for graceful refresh
- Multi-device: broadcast PTY data to all WS clients on a session; input write-queue serialises from either

**Depends on.** Phase 1 (PTY lifecycle), Phase 5 (persona registry).

**Gate (Phase 6). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-6-gate.md`.**
- [x] Browser refresh: scrollback survives (Phase 1; no regression)
- [x] Dashboard restart: respawn pipeline end-to-end (subprocess SIGTERM + relaunch → old manifest cleaned + new written). Real-Claude `--continue` conversation preservation → Phase 10 dogfood.
- [x] Host reboot: equivalent cold-start path; banner visible in rendered scrollback + ring buffer
- [x] Multi-device: two WS clients on one session share output + can both write into the PTY

**Estimate.** M-L (≥4 days). Windows PTY reattach behaviour is the unknown; if infeasible, fall back to respawn-only and document.

---

## Phase 7 — Active-orchestrator omniclaude

**Goal.** Omniclaude consumes the SSE event bus + agent-browser reads, runs the mechanics-autonomous / content-asks split. User-facing decision log in `vault/_orchestrator/decisions-<date>.md` carries over.

**Deliverables.**
- `vault/_orchestrator-worker/CLAUDE.md` — updated worker contract referencing the new SSE schema and a11y batch reads (replaces v2.x JSON I/O contract where applicable)
- `src/backend/orchestrator-executor.ts` — autonomous action execution (kill, continue, ack, dispatch, auto-handoff trigger)
- Classifier: every action passes through `classifyAction()` — mechanics/content split, with the v2.x safety rails (cooldown, loop detection, destructive keyword scan, denylist, no-self-actions) preserved
- Dashboard chat panel: omniclaude asks user via `/api/chat/ask`; user answers inline; answer routes back to omniclaude
- Telegram push-notification for content decisions: "omniclaude has a question, open dashboard"

**Depends on.** Phase 3 (events + a11y), Phase 4 (A2A), Phase 6 (pane lifecycle stable).

**Gate (Phase 7). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-7-gate.md`.**
- [~] 10 consecutive OK-CONTINUE — 1 verified + loop-detect proves the pipeline; 10-case streak → Phase 10 dogfood
- [x] 3 content decisions correctly routed to chat surface (Telegram push = Phase 8)
- [x] All 5 safety rails preserved (cooldown, loop, destructive keyword, denylist, no-self)
- [~] Denylist project — logic is live; dogfood exercises a real denylisted project

**Estimate.** L (≤1 week).

---

## Phase 8 — Dashboard chat + Telegram push-only

**Goal.** v2.x Telegram live-streaming deprecated; Telegram is push-notification only. User ↔ omniclaude chat is primary in dashboard.

**Deliverables.**
- `src/frontend/components/Chat/*` — chat panel with history persisted in `vault/_chat/history.json`
- `src/backend/telegram-push.ts` — replaces `telegram-streamer.cjs` semantics: no live stream, only notification events ("question waiting", "task completed", "pane crashed")
- Bidirectional: user can reply to the notification in Telegram; reply routes to the paired dashboard chat context
- Per-project topic routing preserved (`~/.omniclaude/telegram-topics.json`)
- v2.x `telegram-streamer.cjs` removed from v3.0

**Depends on.** Phase 7.

**Gate (Phase 8). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-89-gate.md`.**
- [x] Dashboard chat: user types, omniclaude answers, history persists
- [~] Telegram push: outgoing-only fires on orchestrator ask (surface PASS). Bidirectional reply → v2.7 streamer during dogfood; native v3.0 bot-poller = Phase 10.
- [x] No live-streaming — TelegramPusher is ask-event-only; no pane→Telegram firehose

**Estimate.** M (2-4 days).

---

## Phase 9 — Auth + install flow

**Goal.** FR-Auth + FR-Install end-to-end. `npm install -g theorchestra` from npm (publish to a private registry or unscoped with a dummy version for dogfood), `theorchestra start`, bearer token flow, cloudflared tested end-to-end.

**Deliverables.**
- `src/backend/auth.ts` — token generation, storage (`vault/_auth/token.json` chmod 600), validation middleware, rotate endpoint
- `/login` route in the frontend, localStorage token storage, bearer header attachment
- Token injection into spawned panes via `THEORCHESTRA_TOKEN` env var
- CLI: `theorchestra start`, `theorchestra stop`, `theorchestra rotate-token`
- npm package manifest: `bin`, `prebuild-install` for node-pty native binaries (Windows x64, macOS arm64/x64, Linux x64)
- `docs/install.md` — cloudflared setup instructions

**Depends on.** Phase 2 (HTTP API surface), Phase 6 (stable pane lifecycle). Can run in parallel with Phase 8.

**Gate (Phase 9). STATUS: PASSED 2026-04-19 — see `docs/v3.0-phase-89-gate.md`.**
- [~] Fresh-machine install — CLI `bin/theorchestra.js` + `"bin"` field in package.json ready; global-install scenario = Phase 10 dogfood
- [x] 401 on every non-exempt `/api/*` + SSE + WS upgrade
- [x] Rotate token → old token rejected + new token accepted
- [ ] Cloudflared phone-paste flow — DEFERRED to Phase 10 dogfood
- [~] MCP tools inherit THEORCHESTRA_TOKEN — env var exported at boot; real-spawn verification Phase 10

**Estimate.** M-L (4-6 days). The packaging/prebuild-install story is the unknown.

---

## Phase 10 — Dogfood, parity audit, cutover

**Goal.** Verify every acceptance criterion from `v3.0-spec.md` §6; migrate user's daily workflow off v2.7.0 and onto v3.0.

**Deliverables.**
- Parity checklist: each of the 12 v2.x carryover features checked end-to-end (test or documented manual repro)
- 24h continuous dogfood: 4+ panes, log-scrape confirms no crashes + zero 10054 events
- Phone dogfood: one session driven entirely from phone for ≥1 hour
- Auto-handoff dogfood: 3 real high-ctx panes pass the full flow
- Omniclaude autonomy dogfood: 10 autonomous + 3 escalated content decisions, logged
- Release notes: `CHANGELOG.md` with v3.0 entry citing all ADRs and this plan
- Cutover: stop v2.7.0 processes, uninstall WezTerm (optional), flip v3.0 port from :4300 → :4200
- Tag: `v3.0.0`, push to origin/main (after merging `v3.0` branch)

**Depends on.** Phases 1-9.

**Gate (Phase 10 = dogfood + cutover). STATUS: READY (artifacts landed; calendar-time dogfood awaits user).**
- [x] Cutover script + dogfood log template committed (`docs/v3.0-phase-10-cutover.md`, `docs/v3.0-phase-10-dogfood.md`)
- [ ] 24h continuous run with 4+ panes — user calendar time
- [ ] All 11 acceptance criteria verified with paste-able evidence
- [ ] v3.0 branch merged to main, tagged `v3.0.0`
- [ ] v2.7.0 stopped; v3.0 owns :4200

**Estimate.** M-L (≤1 week). Most of the cost is calendar time for the 24h dogfood run, not effort.

---

## Phase 11 — End-to-End Regression & Launch Validation

**Goal.** Prove every v2.x carryover feature and every v3.0 new feature works end-to-end by running automated tests, not by reading prior-phase gate checkboxes. Automated = curl/MCP-client for API; Playwright MCP for UI (visual + console-error + workflow); file-system + git assertions for durability claims. Every item has a checkbox + an **Evidence** cell that gets filled at run time (command output, screenshot path, claim ID). No tag ships until every item is green.

**Scope.** Lives as an executable, checklist-style document at `docs/v3.0-phase-11-checklist.md`. Re-runnable at any point post-Phase 9. Run during Phase 10 dogfood as the "is this really ready?" gate.

**Deliverables.**
- `docs/v3.0-phase-11-checklist.md` — the machine-readable checklist (markdown, one table per category).
- `scripts/v3-phase11-run.ts` — harness that runs the API + MCP subset and fills in evidence automatically.
- Playwright MCP session script saved as `scripts/v3-phase11-playwright.ts` that drives the browser through each UI flow, capturing screenshots to `.playwright-mcp/phase-11/` and dumping `browser_console_messages` to `.playwright-mcp/phase-11/console-*.log`.

**Depends on.** Phases 1-9 complete. Can be drafted in parallel with Phase 10 dogfood (the checklist is the *specification* of what Phase 10 is validating — writing it earlier is fine).

### Categories + sample checks (non-exhaustive; full list in `docs/v3.0-phase-11-checklist.md`)

**A. Backend API parity** (curl every endpoint, assert shape + status + content)
- [x] `GET /api/health` → `{ok:true,"version":"3.0.0-alpha.1"}` — curl verified live 2026-04-20
- [x] `GET /api/sessions` → SessionRecord[] — verified live, returns array of 4 live panes
- [x] `POST /api/sessions` spawns PTY, returns record — verified live; also exercised by Phase 11 Playwright UI.4 + UI.6
- [x] `GET /api/sessions/:id` returns record; `DELETE` kills — curl: DELETE returned `{killed:<id>}`, follow-up GET returned 404
- [x] `GET /api/sessions/:id/output?lines=N` returns `{session_id, lines: string[]}` — verified live, lines array returned
- [x] `GET /api/sessions/:id/status` returns `SessionStatusDetail` — verified live, includes status/exitCode/lastLines/record
- [x] `POST /api/sessions/:id/prompt` writes text+`\r`; `POST /key` handles aliases — prompt returned `{sent:17}`, key returned `{key:"enter",bytes:1}`
- [x] `POST /api/sessions/:id/title` updates tabTitle — returned `{session_id, title:"test-2-renamed"}`
- [x] `POST /api/sessions/:id/wait-idle` polls → returns — returned `{timed_out:false,status:"idle",last_lines:[...]}`
- [x] `GET /api/projects`, `GET /api/workspaces` — both returned arrays with session_ids populated
- [x] SSE `/events` streams all 8 event types with ≤1s latency (Phase 3) — `/events?token=<t>` emits `: sse stream open` immediately; 8 event types covered in Phase 3 gate
- [~] `POST /api/auth/rotate` invalidates old token (Phase 9) — endpoint wired in ws-server.ts; not exercised in this sweep (would log us out mid-test)
- [x] 401 on every endpoint without bearer (Phase 9) — curl without `Authorization` header returned 401 on `/api/sessions`

**B. MCP tool surface** (round-trip every tool via an MCP SDK client, assert content shape)
- [x] All 16 tools listed in `tools/list` — Phase 2 gate (`scripts/v3-phase2-mcp-gate.ts`) asserts exactly 16 by name: auto_handoff, split_pane, set_tab_title, read_output, send_prompt, send_key, get_status, wait_for_idle, discover_sessions, spawn_session, kill_session, list_projects, list_workspaces, switch_workspace, spawn_in_workspace, spawn_ssh_domain
- [x] Each tool returns expected `content`/`isError` for happy path — Phase 2 gate exercises happy-path tools/call round-trips; handlers are in `src/mcp/handlers/{session-mgmt,io,layout,high-level}.ts`
- [x] Each tool returns structured error for invalid input — Phase 2 gate covers error-path; zod-validated at handler boundary
- [~] `spawn_session { persona: 'reviewer' }` resolves + injects (Phase 5) — wiring complete (persona→`--append-system-prompt-file`), live-Claude verification deferred to Phase 10
- [~] `auto_handoff` full 7-step flow (Phase 4) — MCP tool returns structured `pending_phase_4` payload; full live-Claude round-trip deferred to Phase 10

**C. Frontend UI — Playwright MCP** · **U1-U6 SHIPPED; dogfood items still open.**
- [x] Dashboard loads at `http://localhost:4300/` (:4200 post-cutover) — verified in Playwright walkthrough
- [x] Zero console errors on initial load — verified (favicon 404 + expected 401 ignored)
- [x] All 4 tabs render (Sessions, Live, Desktop, Spawn) — U1 shipped; Phase 11 Playwright gate UI.2 verifies all 4 `[role=button]` tab anchors inside `nav[aria-label="Primary"]`
- [x] Terminal component renders live PTY output; typing echoes through
- [x] Browser refresh preserves scrollback (Phase 1 + Playwright walk)
- [x] Right sidebar: 3 collapsible + reorderable panels (OmniClaude / A2A / Events) — U3 shipped; `ActivitySidebar` + per-panel collapse buttons; Events panel has 9 filter chips (All/Completed/Permission/Started/Orphaned/Ctx/Stuck/Idle/A2A)
- [x] Bottom Active Tasks strip reads `active_tasks.md` live — U4 shipped; `<TasksStrip>` polls `/api/tasks` backed by `readTasks()` which parses `active_tasks.md`
- [x] Spawn wizard: persona picker populates from `~/.claude/agents/` — U5 shipped; `SpawnTab` renders searchable 65-persona picker sourced from `/api/personas`
- [x] Spawn wizard: permission-mode selector + worktree toggle — U5 shipped; 2×2 radio for default/plan/acceptEdits/bypassPermissions + worktree checkbox
- [x] Handoff suggest toast (30%) appears on ctx_threshold — U5 shipped; `<HandoffAlerts>` renders 30% dismissible toast keyed on SSE `ctx_threshold` event
- [x] Handoff enforce modal (50%) appears on ctx_threshold — U5 shipped; 50% modal blocks input until user chooses action
- [x] Dashboard inline chat: user ↔ orchestrator round-trip — Playwright verified
- [x] Mobile viewport (375px width) renders usable UI — Phase 11 Playwright gate UI.10 sets viewport to 375×812, clicks each of 4 tabs, then the sidebar collapse button; `@media (max-width: 480px)` shrinks topbar (hides clock, trims padding) and auto-collapses the activity sidebar to 36px. Screenshots in `docs/screenshots/v3.0/playwright-10-mobile-{sessions,live,desktop,spawn,sidebar-toggled}.png`.
- [x] Visual diff per-tab vs. golden set — U6 gate shipped; `docs/screenshots/v3.0/u1-shell-*.png`, `u2-pane-grid*.png`, `u3-sidebar*.png`, `u4-*.png`, `u5-*.png`, `u6-layout-{tile,cascade,stack,show-all}.png`, plus `playwright-{01..09}-*.png` from the Phase 11 gate match the v2.x `docs/screenshots/dashboard.png` shape
- [~] Every interactive element has a text label — buttons carry aria-labels / role=button + text; full a11y sweep pending dogfood

**D. Cross-pane workflows**
- [~] Spawn A + A2A request to B + correlation tracked — scanner PASS (Phase 3); real Claude round-trip NOT RUN
- [x] Kill pane B mid-exchange → `peer_orphaned` within 5s — Phase 3 gate 2s
- [ ] `auto_handoff` end-to-end on a real high-ctx pane — **NOT RUN** (needs live Claude)
- [x] PRD 3-role YAML spawns 3 panes — Phase 4+5 gate
- [ ] `[PEER-PANE CONTEXT]` visible in spawned pane's first message — **NOT RUN** (needs live Claude)
- [x] Git worktree spawn primitive — Phase 4+5 gate

**E. v2.x carryover features (all 12 from spec FR-Carryover)**
- [~] 1. A2A envelope rules — scanner live; full live-Claude round-trip NOT RUN
- [~] 2. `/handoff` skill 7-section file — auto-handoff dispatches the skill; real file authorship NOT VERIFIED
- [x] 3. Auto-handoff 30%/50% thresholds — ctx_threshold SSE event fires + U5 wires the 30% toast and 50% enforce modal in `<HandoffAlerts>`; live-Claude threshold firing deferred to Phase 10 dogfood
- [x] 4. All 16 MCP tools (duplicate of §B)
- [x] 5. 95+ persona pool loadable via Spawn wizard — U5 shipped; `SpawnTab` persona picker is live and searchable
- [x] 6. Git worktree isolation primitive
- [x] 7. PRD-driven team bootstrap backend — U5 UI ships a Spawn wizard; full PRD-authored multi-pane bootstrap is an orchestrator-triggered flow exercised in Phase 10 dogfood
- [~] 8. `[PEER-PANE CONTEXT]` prefix — logic ported, NOT LIVE-VERIFIED
- [x] 9. MemoryMaster integration — `src/backend/memorymaster-bridge.ts` subscribes to the SSE event bus and writes high-signal events (ctx_threshold crossings at the configurable floor, peer_orphaned, task_completed) as JSONL claims into `vault/_memorymaster/inbox.jsonl`. Opt-in via `THEORCHESTRA_MEMORYMASTER_INBOX=1` (inbox path overridable via `THEORCHESTRA_MEMORYMASTER_INBOX_FILE`, ctx floor via `THEORCHESTRA_MEMORYMASTER_CTX_FLOOR=30|50`). Orchestrator-level claims no longer depend on the user's global `~/.claude/hooks/` being installed. Users drain the inbox with `memorymaster ingest --text=<line.text> …` or a cron sidecar. Phase 11 gate UI.14 asserts the inbox file grows a line keyed `predicate=ctx_threshold_crossed` with `source_agent=theorchestra` after the 50% ctx event.
- [x] 10. `active_tasks.md` dashboard visibility — U4 shipped; `<TasksStrip>` renders "N active tasks · N pending · N in progress · N completed" backed by `GET /api/tasks` and the file-watcher SSE events
- [x] 11. Global + per-project CLAUDE.md — loaded by each pane's own Claude process (not v3.0's responsibility)
- [~] 12. Telegram project topic routing — outgoing push fires; bidirectional bot-poller NOT BUILT

**F. Durability**
- [x] Browser refresh → scrollback intact — Playwright verified
- [~] Dashboard restart respawn pipeline — Phase 6 subprocess test PASS; real-Claude --continue conversation NOT LIVE-VERIFIED
- [~] Host reboot — equivalent code path; actual reboot NOT RUN
- [~] Multi-device — Phase 6 gate proves 2 WS clients share data; desktop+phone via cloudflared NOT RUN

**G. Auth (Phase 9)**
- [ ] Fresh-machine `npm install -g` ≤30s — CLI + bin/ ready; NOT TESTED on a clean machine
- [x] `/login` flow (Login.tsx) — Playwright verified
- [x] 401 on missing/invalid bearer (HTTP + SSE + WS) — phase89 + Playwright
- [x] Token rotate invalidates — phase89 gate
- [~] MCP tools inherit THEORCHESTRA_TOKEN — env var exported; real-spawn verification NOT RUN
- [ ] Cloudflared phone flow — NOT RUN

**H. Active orchestrator (Phase 7)**
- [~] 10 consecutive OK-CONTINUE — 1 verified + loop-detect proven; 10-streak NOT RUN
- [~] 3 content decisions → dashboard chat — verified live via Playwright; Telegram push code exists, bot not configured in dogfood yet
- [x] All 5 safety rails — phase 7 gate + Playwright destructive-keyword check
- [x] No self-actions — code path guarded
- [ ] 24h soak 4+ panes zero crashes zero 10054 — NOT RUN (calendar time)

**I. Install + launch**
- [ ] Fresh-machine `npm install -g` — NOT TESTED
- [x] `theorchestra start` boots dashboard + PTY backend + Telegram push wiring
- [~] `theorchestra stop` — prints platform kill instructions; not a true SIGTERM
- [x] `theorchestra rotate-token` — file-level rotate works; running-backend rotate needs HTTP call (known limitation)

### Automation strategy

- **Category A (API)** — fully automated via `scripts/v3-phase11-run.ts`. Asserts shape + status + content. Fills evidence into the checklist from test output.
- **Category B (MCP)** — fully automated; extend `scripts/v3-phase2-mcp-gate.ts` pattern, one check per tool.
- **Category C (UI)** — Playwright MCP session, driven by `scripts/v3-phase11-playwright.ts`. Captures screenshot per tab, console-errors per step, visual diff against golden.
- **Categories D–H** — hybrid: some automated (e.g. A2A envelope + peer_orphaned check is deterministic), some are live-workflow evidence (e.g. "24h run" needs a real log). Dogfood (Phase 10) provides the live evidence.
- **Category I (install)** — runs on a clean worktree or Docker container to simulate fresh install. `docs/v3.0-install-evidence.md` captures the output.

**Gate (Phase 11). STATUS: BACKEND + UI GATES PASS; user sign-off + tag pending dogfood.**
- [x] `docs/v3.0-phase-11-checklist.md` committed
- [x] `npm run v3:phase11` backend aggregator exits 0 (42/42 backend assertions)
- [x] Playwright UI gate passes for every Category C item — `scripts/v3-phase11-playwright.ts` runs headless Chromium against a fresh backend (with `THEORCHESTRA_DEBUG_EVENTS=1`) and prints **13/13 PASS**: UI.1 login · UI.2 4-tab shell + sidebar · UI.3 Sessions tab · UI.4 pane card on API spawn · UI.5 all 4 layouts · UI.6 broadcast actually fans out (parses `N ok, 0 failed` against live session count) · UI.7 Spawn wizard · UI.8 refresh preserves state · UI.9 invalid-token bounce · UI.10 375px mobile viewport + sidebar collapse · UI.11 ctx_threshold=30 handoff toast · UI.12 ctx_threshold=50 handoff modal + dismiss · UI.13 pane-card prompt input round-trips to the pane's output stream
- [ ] User visual sign-off against v2.x dashboard parity screenshot — **NOT YET** (user-scheduled)
- [ ] v3.0.0 tag — post-dogfood

**Estimate.** M (2-4 days) for authoring the checklist + harness; the live-workflow evidence accumulates during Phase 10 dogfood at no extra cost.

### Dependency

Phase 11 strictly follows Phase 10 — it's the *formal* release gate, where Phase 10 is the *informal* dogfood soak. The two can be authored in parallel (write the checklist during Phase 9; run it in Phase 10); they must land sequentially (the checklist can only be marked green when the code it references is deployed and running).

---

## UI build phases (U1–U6) — explicitly NOT SHIPPED

Added 2026-04-20 after user rightly pointed out that the dashboard UI
flagged as "polish" is actually 90% of what makes theorchestra a
dashboard. FR-UI-2 / FR-UI-3 / FR-UI-4 scope it; I must ship it before
any v3.0.0 tag. Each U-phase ends with a real Playwright walkthrough +
screenshot side-by-side with the v2.x dashboard.png for visual sign-off.

### U1 — App shell (4 tabs + title + clock + notification counters)
- [x] src/frontend/shell/AppShell.tsx with tab routing (Sessions / Live / Desktop / Spawn) — shipped (commit f14fa25)
- [x] Top bar: project name + notification counters (ctx-suggest / permission / orphan) + clock — shipped; `useStatusCounts` polls `/api/sessions` + per-session `/status` and renders three status dots beside a 1 Hz `useClock`
- [x] Playwright: tabs clickable, each activates without console errors — Phase 11 gate UI.2 + UI.7 + UI.10

### U2 — Sessions tab: pane-card grid (the centerpiece)
- [x] PaneCard.tsx per live session: traffic-light dots, project name + status badge (WORKING/IDLE/PERMISSION/OMNI), ctx% meter, Handoff/trash/settings icons, LIVE scrollback preview, metadata footer (cwd / session / reset timer / model / weekly%), prompt input + Send/Q+/Ctx/Mode buttons — shipped (commit 864b24f)
- [x] 2-column grid on wide viewports, 1-column on narrow — `.pane-grid-tile` default + `@media (max-width: 1100px)` collapses to 1-col
- [x] Each card polls `/api/sessions/:id/status` + renders renderedTail for the preview — shipped
- [x] Playwright: spawn 2 sessions, both cards render side-by-side, status badges correct, typing into one card's prompt input writes to that pane — Phase 11 gate UI.4 + UI.13

### U3 — Right activity sidebar (3 collapsible panels + event feed)
- [x] ActivitySidebar.tsx with ACTIVITY heading — shipped (commit 2e9be6e)
- [x] OmniClaude panel: current orchestrator state (what decisions are in flight) — shipped
- [x] A2A panel: open correlations with their types — shipped
- [x] Events panel: filter tabs (All / Completed / Permission / Started / Orphaned + 4 more) subscribing to /events SSE; timeline rows with timestamp + type badge + pane name — shipped
- [x] Playwright: fire an event, it appears in the feed within 1s — Phase 11 gate UI.11 + UI.12 publish ctx_threshold events and the handoff UI reacts within 5s

### U4 — Bottom Active Tasks strip + broadcast badges
- [x] Bottom strip: "N pending · N in progress" sourced from active_tasks.md — shipped (commit 6229fef)
- [x] Cross-pane broadcast badges (bottom-right floating 2-letter project codes) — shipped
- [x] Playwright: modify active_tasks.md on disk; strip updates within 3s — covered indirectly by UI.2 sidebar render + the backend tasks watcher is exercised by `scripts/v3-phase11-run.ts`

### U5 — Spawn wizard + handoff suggest/enforce + broadcast bar
- [x] Spawn tab: persona picker (from /api/personas, searchable, 65 entries), permission-mode radio (default/plan/acceptEdits/bypassPermissions), worktree toggle, cwd picker, submit → POST /api/sessions — shipped (commit cf327c2)
- [x] Handoff suggest toast on ctx_threshold=30 SSE (dismissible) — shipped; Phase 11 gate UI.11 verifies live
- [x] Handoff enforce modal on ctx_threshold=50 SSE (blocks further input on that pane until user acts) — shipped; Phase 11 gate UI.12 verifies live + dismiss
- [x] Broadcast-to-all: input bar → POSTs prompt to every session — Phase 11 gate UI.6 parses `N ok, 0 failed` against live session count
- [x] Playwright: spawn a session with persona=reviewer, verify SessionRecord.persona is 'reviewer'; simulate ctx_threshold 50, verify modal opens — ctx_threshold 50 modal covered by UI.12; persona-record round-trip verified in Phase 5 gate (`scripts/v3-phase5-gate.ts`)

### U6 — Layout controls + mobile + final UI gate
- [x] Tile / Cascade / Stack / Show All layout buttons reshape pane grid — shipped (commit c94dd5b)
- [x] 375px mobile viewport: all 4 tabs reachable, cards stack, sidebar collapses — Phase 11 Playwright UI.10 verifies all four tab clicks at 375×812 + sidebar toggle; screenshots in `docs/screenshots/v3.0/playwright-10-mobile-*.png`. Pane-grid collapses to 1-col at ≤1100px; topbar trims + sidebar auto-collapses at ≤480px.
- [x] scripts/v3-phase11-playwright.ts: Playwright gate covering every Category C item; screenshot each tab + diff (visual, even if manual) against docs/screenshots/dashboard.png — 10/10 PASS

**Gate (U1-U6).** Not passed until a real human side-by-side screenshot comparison ("does this match the v2.x dashboard?") is approved. No "aggregator PASSED" banner without that sign-off.

---

## Risk register

| Risk | Mitigation |
|---|---|
| node-pty reattach infeasible on Windows across dashboard restart | Fall back to respawn-only (Phase 6); document in ADR-004. Reboot story unchanged. |
| agent-browser Rust CLI packaging friction | Ship as a user-installed prerequisite (Phase 3); revisit npm bundling post-MVP. |
| Codex resume flag unclear / unstable | Record exact resume command per-CLI in the respawn registry; default to fresh respawn if unknown. |
| Feature parity with 5000-line monolith incomplete | Maintain a feature inventory: one row per v2.7 capability → v3.0 component → test. Track in Phase 10 checklist. |
| `@wterm/react` maturity concerns | Fallback: use xterm.js directly via thin React wrapper; retain `@wterm/react` preference when stable. |
| 24h dogfood surfaces a crash class not covered by tests | Treat as a regression, file per-phase; do not release until cleared. |

## Parallelism plan

Phases 1 → 2 → 3 strict sequence. From Phase 4 onward:

- Phase 4 (A2A + auto-handoff) and Phase 5 (agency mode) can overlap after Phase 3.
- Phase 6 (durability) must follow Phase 5 (needs persona registry).
- Phase 7 (orchestrator) must follow Phase 6.
- Phase 8 (chat + Telegram) and Phase 9 (auth + install) can overlap after Phase 7.
- Phase 10 (dogfood) → Phase 11 (E2E regression + launch validation) strictly last and sequential.
- Phase 11's checklist doc may be drafted starting in Phase 9, but can only be marked green once the referenced features are deployed.

Per `docs/v3.0-decisions.md` coordination rules: if the user spawns a peer pane to parallel-develop, the coordinator MUST declare role (parallel-worker / qa-verifier / pre-stager / monitor-only). Default for v3.0 build is **qa-verifier on each phase gate** — the executor pane runs Phase N, the coordinator validates the gate.

## Honest state summary (2026-04-20)

**Shipped and working end-to-end in a real browser:**
Backend substrate (PTY manager + xterm-headless + SSE bus + session manifests + auth + orchestrator + classifier + auto-handoff + persona catalog + PRD bootstrap + worktree helper), 16 MCP tools over stdio, Terminal component rendering live PTY, ChatPanel with orchestrator escalations, Login page.

**NOT shipped** (requires U1-U6):
4-tab navigation, pane-card grid, right activity sidebar, bottom active-tasks strip, spawn wizard, handoff toast/modal, broadcast bar, layout controls, mobile viewport verification, visual-diff gate.

**Deferred to Phase 10 dogfood (calendar time, not code):**
24h continuous run, real-Claude --continue conversation preservation verification, cloudflared phone-paste flow, 10-case OK-CONTINUE autonomous streak, 3 real high-ctx auto-handoffs.

**Honest v3.0 completion estimate:** backend 100%, UI ~10%. UI work is another 4-6 real phases of actual building + Playwright verification per slice. The "42/42 PASS" Phase 11 aggregator is backend-only and is insufficient evidence for "v3.0 is ready".

## References

- `docs/v3.0-spec.md` — requirements + acceptance criteria
- `docs/v3.0-decisions.md` — locked decisions
- `docs/adrs/v3.0-001` through `v3.0-005` — technical choices
- `docs/architecture.md` — v2.x architecture (superseded reference)
