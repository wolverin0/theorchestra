# theorchestra v3.0 — Locked Decisions

Captured 2026-04-19 after structured scope-defining conversation. These are the architectural commitments that anchor the v3.0 spec and phased plan. Any change to any of these requires explicit user sign-off.

## Philosophy

Build the **most robust AI-orchestration substrate possible**, without restraint on time or scope. v3.0 replaces v2.7.0. v2.7.0 stays running in parallel on the user's machine until v3.0 is fully dogfooded and cut over.

---

## 1. Substrate — DROP WEZTERM ENTIRELY

- All terminals (regular shell AND AI-orchestration panes) live inside **wterm** panes rendered in the dashboard at `http://localhost:4200`.
- **No Windows Terminal bifurcation.** The whole point is full portability: user works at home, closes laptop, reopens on phone, keeps typing into the same pane. Windows Terminal/cmd/PowerShell/WSL are not used as shell hosts for v3.0 work.
- WezTerm is not required on the machine. Existing v2.7.0 installation stays during the transition, uninstalled once v3.0 is battle-tested.
- Kills the entire 10054 error class at the architectural level (no per-call `wezterm cli` spawns).

## 2. Omniclaude — ACTIVE ORCHESTRATOR

**Role change**: from passive router + event observer (v2.x) → active architect/orchestrator (v3.0).

**Autonomy split**:
- **Mechanics = autonomous** (decides without asking): "OK CONTINUE" prompts, auto-handoff when ctx fills, acking peer A2A messages, dispatching queued tasks per roadmap order, killing stuck panes, approving routine tool-use permission prompts.
- **Content = asks**: accept/reject a design decision, merge to main, feature direction choices, cross-project spawns, anything marked critical.

**Input corpus** (what omniclaude uses to decide):
- **agent-browser a11y-tree reads** against each pane's wterm rendering — deterministic, structured, no regex scrollback scraping.
- **Dashboard SSE events** — semantic events like `pane_idle`, `permission_prompt`, `peer_orphaned`, `ctx_threshold`, `a2a_received`. Push-based, real-time.
- Both correlated for robust decisions.

**User ↔ omniclaude chat**:
- Dashboard inline chat panel is primary.
- Telegram becomes **push-notification only**: "omniclaude has a question, open dashboard". No more terminal streaming to Telegram topics. Live streaming deprecated.
- Omniclaude conversation lives in dashboard; user never has to check two places.

## 3. Backend Stack

- **PTY backend**: Node.js + `node-pty` (ConPTY on Windows). Ships in the same process as the dashboard server. Matches existing wezbridge codebase language; mature library (VS Code, Hyper, Tabby use it).
- **UI framework**: `@wterm/react` + Vite. Port the current single-file 5000-line HTML into proper React components. No SSR (admin UI, doesn't need it).
- **Persistence**:
  - Close browser tab → pane survives (PTY keeps running in backend)
  - Restart dashboard → reconnect to running PTYs via durable session IDs
  - Reboot → auto-respawn Claude/Codex panes with `--continue` so sessions resume seamlessly
- **Install + launch**: `npm install -g theorchestra` + `theorchestra start` single command launches dashboard + PTY backend + (optional) Telegram integration together.

## 4. Auth + Remote Access

- Keep existing **cloudflared tunnel** for phone/remote access.
- Add a **bearer-token auth check** to the dashboard. Unknown requests → 401. Token saved in user's browser once per device. Protects against random cloudflared URL discovery.
- No OAuth, no multi-user, no user accounts. Single-user tool.

## 5. Multi-LLM Support

- Claude Code, Codex, gemini-cli, droid, opencode — **all first-class**. Any TUI CLI works as a pane. PTY backend doesn't care what process it spawns.
- A2A protocol works identically across LLMs because stdin is stdin. Codex already speaks the envelope format from v2.x.
- Persona system: Claude uses `~/.claude/agents/`; Codex uses its own agent config. Dashboard knows which persona source to pull from based on the CLI being spawned.

## 6. v2.x Carryover — ALL 12 FEATURES SURVIVE

### Core wezbridge protocol
1. **A2A envelope format + rules** — `[A2A from pane-X to pane-Y | corr=... | type=...]`, coordinator role declaration (parallel-worker / qa-verifier / pre-stager / monitor-only), push-vs-watch discipline, mandatory `send_key('enter')` after `send_prompt`.
2. **`/handoff` skill + structured handoff files** — 7-section template (Context / Current State / Open Threads / Next Steps / Constraints & Gotchas / Relevant Files / Corr ID), skill invocation pattern, external-write fallback for ctx-full panes.
3. **Auto-handoff daemon (Ctx-aware reset)** — 30% suggest / 50% enforce thresholds, READY/NOT_READY readiness-check protocol, 120s readiness poll, wait-for-idle-before-`/clear`, Ctrl+C + belt-and-suspenders enters, SSE broadcast.
4. **MCP tool surface (wezbridge namespace)** — `send_prompt`, `send_key`, `read_output`, `spawn_session`, `kill_session`, `auto_handoff`, `discover_sessions`, `split_pane`, `spawn_in_workspace`, `spawn_ssh_domain`, `set_tab_title`, `list_projects`, `list_workspaces`, `switch_workspace`, `wait_for_idle`, `get_status`. Same signatures, new node-pty-based implementation.

### v2.5 Agency Mode
5. **95+ persona pool** at `~/.claude/agents/` — spawn with `persona: 'reviewer' | 'coder' | 'frontend-design' | ...`. Dashboard shows persona badges.
6. **Git worktree isolation per pane** — `git worktree add <path> <branch>` for each pane that edits the same repo. Prevents parallel-agent merge conflicts.
7. **PRD-driven team bootstrap** — YAML file defines roles + tasks, dashboard spawns the whole team in parallel.
8. **`[PEER-PANE CONTEXT]` bootstrap prefix** — v2.7's `spawn_session spawned_by_pane_id` param auto-injects a header telling the spawned pane its own pane_id + coordinator pane_id + A2A reporting pattern. Bridges the persona-written-for-subagent-API gap.

### Memory + tracking + rules
9. **MemoryMaster integration (full stack)** — UserPromptSubmit recall hook, Stop auto-ingest every 15 messages, PreCompact warn-once hook (fixed 2026-04-19 — marker-based, stopped unconditional blocking), `mcp__memorymaster__*` tools on every pane. Cross-session memory is where "best practices" live — non-negotiable for omniclaude's decision quality.
10. **`active_tasks.md` task tracker protocol** — YAML frontmatter per task (status, owner, corr, created_at, dispatched_at, completed_at, stuck_threshold_min, follow_ups). Source of truth for "what's in flight"; omniclaude reads it to prioritize watching.
11. **Global CLAUDE.md rules + per-project overrides** — `~/.claude/CLAUDE.md` (A2A hard rules, coordinator role declaration, Intelligence-First Rule, Peer-Pane A2A Protocol section) + per-project CLAUDE.md files (repo architecture, stack, commands). Every spawned pane reads these at startup.
12. **Telegram project topic routing (bidirectional chat)** — one forum topic per project. Incoming topic-N text routes to pane-K; pane responses go back to topic-N. Bidirectional chat per pane from phone, even though live streaming is deprecated.

## 7. Migration Plan

**Parallel dual-run, gradual cutover.**

1. **v2.7.0 stays running** on wezterm on user's machine. Tag already pushed.
2. **v3.0 opens a new branch** (`v3.0`) off main. Development happens there in isolation.
3. **v3.0 spins up on a different port** (e.g. :4300 to avoid clashing with v2.7.0's :4200). Both can run side-by-side.
4. **Dogfood v3.0 on non-critical panes** for 1-2 weeks. Migrate pane-by-pane, confirm each workflow works.
5. **Once all panes are on v3.0**, stop v2.7.0, uninstall wezterm (or leave it — it doesn't hurt).
6. **Zero-downtime.** Reversible at any step.

## 8. Deferred for v3.0 Phase 2 (NOT in MVP)

- SSO via Cloudflare Access (token-auth covers MVP)
- Tailscale mesh alternative to cloudflared
- Multi-machine / remote PTY backends over SSH
- Full durable systemd-style supervisor (the node-pty-in-dashboard-process model is enough for MVP)
- Subagent support (`Task` tool integration — orthogonal)

## 9. Release Criteria — "v3.0 is done when..."

(TBD — define in next round when writing the spec.)

Candidates to propose:
- All 16 MCP tools have parity with v2.7.0 implementations
- All 12 carryover features verified by test or manual dogfood
- 24h of continuous dogfood on 4+ panes without a crash
- Dashboard renders + accepts input from phone via cloudflared
- Auto-handoff flow successfully tested end-to-end on at least 3 real high-ctx panes
- Omniclaude autonomously continues at least 10 "OK CONTINUE" cases without escalation
- Omniclaude correctly escalates at least 3 content decisions to Telegram

---

## Addendum — 2026-04-22 (post-dogfood refinements)

Items below refine (not replace) the 2026-04-19 locked decisions. Each is
motivated by a real failure observed during multi-day dogfood.

1. **Persistent omniclaude pane > one-shot advisor.** Original §2 described
   "active architect/orchestrator". An exploration phase (P2, P6) implemented
   a one-shot `claude -p` advisor per event. It loses context between calls
   and re-pays input tokens. Superseded by **P7 persistent omniclaude pane**
   (a Claude Code session spawned once in `vault/_omniclaude/`, reused
   turn-over-turn via `--continue`). One-shot advisor stays as degraded-path
   fallback. Opt-in via `THEORCHESTRA_OMNICLAUDE=1`.

2. **Auto-respawn-on-boot is opt-in.** §3 said "Reboot → auto-respawn…". A
   session-manifest respawn on backend restart pulled in prior-day PTYs that
   had process-group ties to user's external WezTerm instances; SIGTERM
   during Ctrl+C propagated and killed those WezTerms. Default reverted to
   OFF. Set `THEORCHESTRA_AUTO_RESPAWN=1` to re-enable. A safer respawn
   path (process-group isolation or PID-allowlist on killAll) is P9 scope.

3. **`THEORCHESTRA_NO_KILL_ON_SHUTDOWN=1` is the recommended default.** For
   the same reason as #2 — Ctrl+C should never touch any PTY. Users who
   want clean PTY teardown can opt-in via unset.

4. **Rule engine is fallback-only when omniclaude is alive.** Originally the
   deterministic rule engine handled every event. With P7, executor.handle()
   short-circuits when `manager.statusDetail(omniSid).status !== 'exited'`.
   Omniclaude owns everything. Rule engine fires only if omniclaude pane
   died. Decisions log still gets entries via omniclaude's own DECISION lines
   parsed out of its PTY output (FR-Orch-5 compliance path, P8.B).

5. **Omniclaude's MCP config is per-cwd, templated at spawn.** Repo-root
   `.mcp.json` leaks into every Claude session spawned under the repo tree
   with stale port/token. Fix: `startOmniclaudeDriver()` writes a dedicated
   `vault/_omniclaude/.mcp.json` with the LIVE port + absolute token-file
   path on each boot. Absolute path is mandatory since the MCP subprocess's
   cwd is `vault/_omniclaude/`, not the repo root.

6. **UI scope extended beyond original "4 tabs + 3 sidebar panels".** Added
   **Omni tab** (surface for the filtered omniclaude pane + direct prompt
   input via `POST /api/orchestrator/tell-omni`) and **Reasoning sidebar
   panel** (advisor/omniclaude state + recent attested decisions). Original
   spec-counted UI surface is a floor not a ceiling.

7. **spawn_session MCP schema extended.** v3 original restricted the MCP
   tool to Claude Code spawns. v2.7 parity (FR-MCP) requires `cli` + `args`
   + `tab_title` for Codex/gemini/cmd.exe/bash etc. Closed in P8.A: when
   `cli !== 'claude'` the generic path spawns any CLI, bypassing persona /
   resume / permission-mode flag logic.

## Dogfood log — 2026-04-22 multi-pane PRD + persona smoke test

`scripts/v3-multi-pane-behavior-gate.ts` ran end-to-end with real Claude Code
panes (Opus 4.7) against isolated tmp-backend. Artifacts archived in
`docs/screenshots/tests/multi-pane-2026-04-22/`.

| # | Scenario | Result | Time | Evidence |
|---|---|---|---|---|
| 1 | Persona injection proof | PASS | 16s | "coder"/"engineer" keyword detected in probe pane scrollback |
| 2 | PRD-bootstrap spawns 3-role team | PASS | 2s | 3 panes w/ personas coder + dev-backend-api + reviewer |
| 3 | A2A envelope + scanner | PASS | 40s | scanner caught 5 events with matching corr |
| 4 | Dependency sequencing (reviewer waits) | PASS | 20s | frontend@0s, backend@0s, **review@20s** — reviewer's bash while-loop blocked correctly |
| 5 | Omniclaude reasons-from-PRD spawn | SKIP | 300s | omniclaude didn't spawn 3 persona panes in 5min — Path B deferred (Path A proves the mechanism) |
| 6 | Real deliverables on disk | PASS | 0s | frontend.html (613B), backend.py (396B), review.md (1.8KB) all present with expected content |

**Bug found + fixed during gate run**: `POST /api/prd-bootstrap` was parsing
`role.prompt` from the YAML but never delivering it as an initial user
message to the spawned pane. Panes booted Claude, sat idle forever. Fix:
pending-prompts array + setTimeout 8s + `manager.writeAndSubmit(sid,
prompt, 300)` to dispatch after Claude's TUI boots. Also added
`--dangerously-skip-permissions` to every PRD-spawned Claude invocation.

**Spec compliance closed**:
- FR-Carryover #5 (95+ persona pool) — verified
- FR-Carryover #7 (PRD-driven team bootstrap) — verified + bug fixed
- FR-Carryover #8 ([PEER-PANE CONTEXT] bootstrap) — partial; header only
  applies via MCP `spawned_by_pane_id` path, not via PRD-bootstrap. PRD
  roles must embed A2A envelope format in their own prompt. Documented.

**Spec compliance still open**:
- A-3 (24h continuous dogfood)
- A-4 (cloudflared + phone)
- A-6 (10 consecutive OK CONTINUE autonomously)
- A-7 (3 Telegram escalations) — **NOW CLOSED 2026-04-23** (see Dogfood log below)
- A-10 (npm install on fresh machine)

## Dogfood log 2026-04-23 → 2026-04-24 — UX hardening + Telegram inbound

Multi-session sprint addressing UX friction ("drifted a lot of the path"),
closing the Telegram integration, and polishing the Omni tab to parity with
the user's native Claude Code terminal.

### Shipped (merged to main)

| PR | Commit | Summary |
|----|--------|---------|
| #1 | `4e60763` | P10 context hygiene + briefer + P11 prd-bootstrap fix + A-3/A-6 scaffolds |
| #2 | `a0845ef` | Zero-flag boot — `npm start` auto-loads `.env.local`, defaults omniclaude=1 + no-kill=1 |
| #3 | `6e23153` | Omni tab colour + spacing + keys + Telegram inbound wiring |

### Omni tab — from read-only grey blob to native-terminal parity

Five compounding fixes:
1. **Keys strip + prompt input**: ESC / arrows / Tab / Enter / ^C / 1/2/3 + `Tell omniclaude…` input with Enter-to-send. Closes the "I can't send ctrl+c to the omni pane" UX gap (omniclaude was filtered out of Sessions/Live/Desktop by design).
2. **Alt-screen frame strip**: Claude CLI repaints the full status bar (`Model:`, `Ctx:`, `cwd:`, `bypass permissions`, horizontal rules) on every turn — backend ring-buffers every frame. Filter in `STATUS_BAR_PATTERNS` removes these (status info is already in the pill header).
3. **CR-collapse** — `Slithering… Slithering…` spinner spam dropped from 50+ per line to zero by keeping only the post-`\r` segment per PTY line.
4. **SGR → HTML colour**: compiled `ansiToHtml()` parser preserves 8/16/256/truecolor codes as inline `<span style="color:…">`. Non-SGR escape sequences still stripped via negated lookahead.
5. **Blank-line preservation** — runs of blanks collapse to ONE so bullet/prompt blocks read like the native terminal.

Validated via claude-in-chrome DOM inspection: 46 coloured spans across 40 lines, 0 status-bar boundaries, 0 spinner duplicates, 12 meaningful blank spacers.

### Telegram inbound — A-7 closed

Root cause found: the `claude-plugins-official/telegram` plugin requires the CLI to be launched with `--channels plugin:telegram@claude-plugins-official`. Without it the plugin's bun subprocess receives DMs (shows "typing" indicator) but has no claude session to forward to — symptom was "bot typing forever, no reply".

Fix in `src/backend/omniclaude-driver.ts`: auto-detect installed plugin at `~/.claude/plugins/cache/claude-plugins-official/telegram` or cwd-scoped equivalent, append `--channels` + `--dangerously-skip-permissions` unless opted out via `THEORCHESTRA_OMNICLAUDE_NO_TELEGRAM=1` / `THEORCHESTRA_OMNICLAUDE_SAFE_MODE=1`.

Also found during diagnosis: Group Privacy mode filters group-chat messages at the Telegram platform level before the plugin ever receives them. DMs work regardless. For group routing: flip Privacy mode off via @BotFather.

Verified end-to-end 2026-04-23: user DM "hola" (message 548) reached omniclaude and got a response ("Test confirmed — your DM 548 arrived as a channel tag, replied as 549") streamed back to Telegram.

### Other closures

- **Sidebar "Type a message" route fix**: was calling `/api/chat/ask` (only appends to ChatStore, never reaches pane). Now posts to `/api/orchestrator/tell-omni` (same as Omni tab input) — enqueueFronts on omniclaude's queue so the LLM actually sees it.
- **Omniclaude cwd migration** — user's pre-paired `.claude/` lives at `G:/_OneDrive/OneDrive/Desktop/Py Apps/omniclaude/`, NOT `vault/_omniclaude/`. Pointed `THEORCHESTRA_OMNICLAUDE_CWD` via `.env.local`. The pre-existing CLAUDE.md there was v2.x-flavoured and had omniclaude chasing a non-existent `omni-watcher.cjs`; patched its "First Turn Protocol" section with a v3.1 migration note.
- **Process hygiene**: rapid-restart cycles accumulated orphan MCP subprocesses + duplicate Telegram plugin bun instances (confirmed getUpdates race condition). Cleanup pattern documented: `powershell Get-CimInstance Win32_Process | Where-Object {$_.CommandLine -match 'theorchestra-mcp|claude --continue|bun.*telegram'} | Stop-Process -Force` before restart.

### Spec compliance deltas

| Gate | Before | After |
|------|--------|-------|
| A-7 (Telegram escalations) | open | **closed** — DM round-trip verified |
| Omni tab usability | read-only grey text | full keys + input + colour + spacing |
| Sidebar Type-a-message delivery | silently dropped | wired via tell-omni |
| omniclaude's scoped .mcp.json | only theorchestra | merged user-global (memorymaster, openclaw2claude) |

### Still open for user

- **A-3 24h soak**: owner = user (needs PC on continuously + `npx tsx scripts/v3-soak-24h-gate.ts`)
- **A-6 10 consecutive OK-CONTINUE**: observer scaffold ready (`scripts/v3-a6-ok-continue-gate.ts`), needs real pane activity
- **P9 Path-B omniclaude-driven PRD**: SKIPed in rounds 1-3. Retry attempted 2026-04-24 overnight run (see next log entry when available).
- **A-4 cloudflared + phone**: not attempted
- **A-10 fresh-machine npm install**: not attempted
- **fork_session** (Claude Code 2.1.117+ supports `--fork-session`): tracked as P12, not urgent — current persona-driven spawn_session pattern is semantically correct for reviewer/builder delegation.

## Next phases

- **Phase B (Research + ADRs)** — deep dive on `@wterm/core`, `@wterm/react`, `@wterm/just-bash`, agent-browser Rust CLI + batch protocol, node-pty ConPTY specifics. Write 3-5 ADRs for pivotal technical choices.
- **Phase C (Spec + Plan)** — full `v3.0-spec.md` (requirements + acceptance criteria) and `v3.0-plan.md` (phased implementation with verification gates).
- **Phase D (Build)** — slice-by-slice per the plan.
